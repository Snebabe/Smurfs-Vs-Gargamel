package com.group9.model;

public class DefenceBoxer extends DefenceEntity{
    public DefenceBoxer(int health, int attackDamage, int range, int level, int cost) {
        super(health, attackDamage, range, level, cost);
    }
}